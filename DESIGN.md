# StrayAtlas 設計說明

> 這份文件描述目前已實作的介面，作為版面重新設計的輸入。
> 語氣與用字以實際畫面上的文案為準——生成新版面時請沿用這些中文字串，不要另行翻譯或改寫。
> 對應的程式碼在 `src/`，色彩 token 在 `src/style.css`。

---

## 1. 這個網站在做什麼

臺灣公立動物收容所「目前仍開放認養」的動物名冊，資料來自農業部開放資料，每日自動更新。

**兩種使用者，看的是同一份資料但問不同的問題：**

| 使用者 | 想知道的事 | 對應畫面 |
|---|---|---|
| 想認養的人 | 「我家附近有哪些貓可以認養？」 | 找動物 |
| 關心議題的人／記者／動保團體 | 「哪個縣市的動物待最久？哪間收容所壓力最大？」 | 縣市地圖、收容所 |

**貫穿全站的一個原則：主動說明資料的限制。** 每個畫面底部都有一塊說明「這張圖不能拿來說什麼」。這不是免責聲明，是內容的一部分——它是這個作品想展示的判斷力，版面上不應該把它縮小或藏起來。

---

## 2. 視覺調性

**溫暖、克制、資訊密度中等。** 動保題材需要親近感，但滯留天數這種冷硬數字放進過度活潑的版面會變成行銷頁。目前的平衡點是：暖色底、圓角、字距寬鬆，但資料呈現本身保持樸素。

- 單一強調色系（橘），沒有第二個彩色
- 無陰影，卡片以 1px 細線分隔
- 無 emoji、無插畫、無漸層
- 中文字型優先 `Noto Sans TC`，回退到系統 UI 字型

---

## 3. 設計 token

**這些值請直接沿用。** 深色模式不是把淺色反轉，是各自選過的一組值，而且橘色階在深色模式下是反向的（深色底上，數值越大顏色越亮）。

### 色彩

| 用途 | Token | 淺色 | 深色 |
|---|---|---|---|
| 頁面底色 | `--plane` | `#f7f2ea` | `#12110f` |
| 卡片／面板 | `--surface` | `#fdfaf6` | `#1a1917` |
| 凹陷區（bar 底槽、hover） | `--surface-sunk` | `#f2ebe1` | `#23211e` |
| 主要文字 | `--ink` | `#1c1917` | `#faf7f2` |
| 次要文字 | `--ink-secondary` | `#57534e` | `#c9c1b6` |
| 弱化文字（標籤、單位） | `--ink-muted` | `#8a8178` | `#948b80` |
| 分隔線 | `--hairline` | `#e6ddd0` | `#332f2a` |
| 無資料 | `--no-data` | `#e9e2d6` | `#2b2824` |

### 資料色階（單色相，六階，數值由小到大）

| 階 | 淺色 | 深色 |
|---|---|---|
| 1 | `#eda17a` | `#824624` |
| 2 | `#e28654` | `#a25223` |
| 3 | `#d66a27` | `#c35e20` |
| 4 | `#b85a21` | `#da7338` |
| 5 | `#984e24` | `#e69062` |
| 6 | `#794224` | `#f1ab88` |

`--ramp-4` 兼作互動強調色：被選中的 chip 底色、bar 填色、tab 底線、focus 外框。

這組色階通過了對比度檢查：單一色相、亮度單調遞增、相鄰階亮度差 ≥ 0.06、最接近底色的那一階對底色仍有 2:1 對比。**若要換色，整組要重新驗證，不能只換其中幾階。**

### 尺寸

| 項目 | 值 |
|---|---|
| 內容最大寬度 | `1180px` |
| 頁面內距 | 上 `3rem`／左右 `1.5rem`／下 `4rem` |
| 卡片圓角 | `12px` |
| 小元件圓角（select、清單列） | `8px` |
| chip 圓角 | `999px`（膠囊） |
| 卡片內距 | `1.25rem` |
| 基礎字級／行高 | `16px` / `1.7` |
| 標題 h1 | `clamp(1.9rem, 4vw, 2.6rem)` |

數字一律使用 `font-variant-numeric: tabular-nums`，否則直向對齊會歪。

---

## 4. 版面骨架

三個頁面共用同一個外殼：

```
┌──────────────────────────────────────────────────────┐
│  StrayAtlas 浪浪地圖              [系統][淺色][深色]  │  ← masthead
│  臺灣公立動物收容所目前仍開放認養的動物。 [資料快照 …] │
├──────────────────────────────────────────────────────┤
│  縣市地圖 │ 找動物 │ 收容所                            │  ← tabs（底線標示當前）
├──────────────────────────────────────────────────────┤
│                                                      │
│                    ← 各頁內容 →                       │
│                                                      │
├──────────────────────────────────────────────────────┤
│  資料來源：農業部「動物認領養」開放資料（dataset …）    │  ← footer
│  本專案程式碼目前保留所有權利…                          │
└──────────────────────────────────────────────────────┘
```

- **標題可點**，回到縣市地圖
- **資料快照日期**是一個膠囊標籤，緊接在副標之後，弱化色
- **主題切換**三段式（系統／淺色／深色），靠右，比一般 chip 小一號
- tabs 為底線式，非膠囊，與篩選用的 chip 明確區分

---

## 5. 畫面一：縣市地圖（`/`）

**目的**：一眼看出動物集中在哪些縣市、哪些縣市待得特別久。

### 版面

左右兩欄等寬。左欄是地圖，**sticky 固定**——右欄清單比地圖高，捲動時地圖跟著讀者往下。

```
[顯示指標: 在所數 | 滯留中位數]   [動物類型: 全部 | 狗 | 貓]

┌─────────────────────┐  ┌─────────────────────────────┐
│  ┌───┐              │  │ 桃園市                       │
│  │連江│      ╭─╮    │  │ 1 間公立收容所                │
│  └───┘     ╭╯ ╰╮   │  │ ─────────────────────────── │
│  ┌───┐    ╭╯   ╰╮  │  │ 在所總數              504    │
│  │金門│    │臺灣 │  │  │ 滯留中位數          1713 天   │
│  └───┘    ╰╮   ╭╯  │  │ 狗            406（80.6%）   │
│            ╰─╮╭╯   │  │ 貓             98（19.4%）   │
│              ╰╯    │  │ 尋獲地含區級          4.8%   │
│                     │  └─────────────────────────────┘
│ ─────────────────── │  ┌─────────────────────────────┐
│ 在所數               │  │ 各縣市在所數                  │
│ ■<65 ■65-148 ■…    │  │ 新北市 ████████████  2,675  │
└─────────────────────┘  │ 臺北市 ███████         978  │
                          │ 高雄市 █████           610  │
                          │ …（22 列）                  │
                          └─────────────────────────────┘
┌──────────────────────────────────────────────────────┐
│ 這張圖不能拿來說什麼                                    │
│ • 縣市＝收容所所在地，不是尋獲地。…                      │
│ • 滯留天數是「目前仍在所」的動物待了多久。…              │
│ • 在所數不等於流量。…                                   │
└──────────────────────────────────────────────────────┘
```

### 地圖細節

- **SVG 向量圖，沒有底圖圖磚**。整張地圖是 22 個縣市多邊形
- 金門、連江畫成左側兩個**插圖框**（細線框＋左下角標籤），因為它們離本島太遠，同框會把本島壓得很小
- 縣市之間以底色描邊隔開（1.4px，`--surface` 色），視覺上像有縫隙
- **hover 行為**：滑到的縣市保持原色，其餘降到 30% 不透明度；點選可鎖定
- 地圖與右欄清單雙向連動——滑清單也會亮地圖

### 細節卡片

**高度固定**（`min-height: 16.5rem`）。滑過不同縣市時內容長度不同，若不預留高度會把下方清單推上推下。無選取時顯示提示文字，垂直置中。

### 圖例

六個色塊＋數值區間，橫排。指標切換時區間會重算（分位數分級，非等距）。

---

## 6. 畫面二：找動物（`/animals`）

**目的**：認養人視角。全國 8,265 隻，用篩選收斂到「我要的那幾隻」。

### 篩選分兩層

```
縣市 [全部（8,265 隻）▾]   收容所 [全部（37 間）▾]
────────────────────────────────────────────────────  ← 分隔線
動物類型 [全部][狗][貓]   品種 [全部（8,265 隻）▾]   已在所 [不限][30天內][31-90天][91-365天][1-2年][2年以上]

符合條件 8,265 隻，依已在所天數由長到短排列。
```

**上層是地區，下層是動物本身的條件**，中間用一條細線分開。縣市與收容所連動——選了縣市，收容所下拉只剩該縣市的。

### 為什麼有的是 chip 有的是下拉

| 控制項 | 形式 | 理由 |
|---|---|---|
| 動物類型（3 項） | chip | 選項少、常切換、當前狀態不用打開就看得見 |
| 已在所（6 項） | chip | 同上 |
| 品種 | 下拉 | 長度不固定：某間收容所 11 種、另一間只有 2 種 |
| 縣市（22）／收容所（37） | 下拉 | 選項太多，攤開會佔掉整個畫面 |

**重新設計時請維持這個分界。** 把三個選項塞進下拉，使用者要三個動作才能完成一次本來一次點擊的事。

### 動物卡片網格

`repeat(auto-fill, minmax(190px, 1fr))`，間距 `0.9rem`，**同列等高**。

```
┌────────────────┐
│                │
│   照片 4:3     │  ← object-fit: cover；無照片時顯示「無照片」
│                │     載入失敗時顯示「照片無法載入」（兩者要能區分）
├────────────────┤
│ 混種犬          │  ← 品種，粗體
│ 母·中型·成體·黑色│  ← 次要色，中點分隔
│ 已在所 4561 天  │  ← 超過一年時附「（約 12.5 年）」
│  （約 12.5 年） │
│ 新北市中和區…    │  ← 僅「找動物」頁顯示，收容所頁省略
│ 103030912      │  ← 收容編號，最弱化
└────────────────┘
```

hover 時上移 2px、邊框轉為 `--ramp-3`。

### 捲動

捲到接近底部自動載入下 48 筆。底部同時保留「再顯示 48 隻（還有 N 隻）」按鈕作為備援，全部顯示完畢後改為「已顯示全部 N 隻」。

---

## 7. 畫面三：收容所（`/shelters`、`/shelters/:id`）

### 列表頁

37 間攤平在同一層，可依縣市篩選（此處用 chip 橫排，因為那份清單本身就是「全臺有哪些縣市」的資訊）。**沒有做「縣市 → 收容所」的下鑽**，因為 14 個縣市只有一間收容所，那一層對它們是空的點擊。

每列是一張可點的寬卡片：

```
┌────────────────────────────────────────────────────────────┐
│ 新北市                                                       │
│ 新北市五股區公立動物之家       725      248      狗 329       │
│ 新北市五股區成泰路…            在所    天中位數   貓 396      │
│                              ████▁▁▁                       │
└────────────────────────────────────────────────────────────┘
```

在所數附一條相對長度的細 bar，讓規模差異一眼可見（最大 2,675 對最小 6）。

### 單一收容所頁

頂部資訊卡（左側名稱／地址／電話，右側四個數字），下方是與「找動物」完全相同的篩選與卡片網格，但少了縣市與收容所兩個下拉。

**地址可能有多行**——三間收容所的來源資料登錄了兩種地址寫法，其中新北市瑞芳區是真的兩個地點。全部列出，並加一行說明，不擇一顯示。

---

## 8. 動物詳細資料（Modal）

點卡片開啟。**不是換頁**，因為使用者可能已經捲過幾百張卡片並設好篩選；換頁再返回會全部失去。但網址會同步變成 `?animal=<id>`，所以連結仍可分享、返回鍵關閉 modal。

```
┌──────────────────────────────────────┐
│                                  [×] │
│                                      │
│          照片 3:2（contain）          │  ← 完整顯示不裁切
│                                      │     無照片時此區塊縮成一條窄帶
├──────────────────────────────────────┤
│ 臺北市 · 臺北市動物之家                │
│ 混種犬                                │
│                                      │
│ 收容編號    流水號     性別            │
│ 103030912  31212     母              │
│ 體型        年齡      毛色            │
│ 中型        成體      黑色            │
│ 絕育        開放認養日  建檔日          │
│ 未絕育      2021-04-11 2014-01-15     │
│ 已在所                                │
│ 4561 天（約 12.5 年）                  │
│                                      │
│ 收容所備註                             │
│ (所內絕育)皮病                         │
│                                      │
│ 欄位內容依各收容所登錄實務而異，未填不代表… │
└──────────────────────────────────────┘
```

最大寬 `680px`、最大高 `88vh`，內部可捲。背景遮罩 `rgba(20,16,12,0.55)`，點擊遮罩或按 Esc 關閉，開啟時鎖住背景捲動。

---

## 9. 狀態

每個資料驅動的畫面都有四種狀態，重新設計時都要有對應樣式：

| 狀態 | 呈現 |
|---|---|
| 載入中 | 純文字「載入中…」，弱化色，上下留白 3rem。找動物頁另註明「（全國動物資料約 272 KB）」 |
| 載入失敗 | 「資料載入失敗（錯誤訊息）。」＋重試按鈕 |
| 篩選後為空 | 「沒有符合條件的動物。」 |
| 尚未選取（地圖細節卡） | 「將游標移到地圖或下方清單上，看該縣市的細節。點選可鎖定。」 |

---

## 10. 響應式

| 斷點 | 變化 |
|---|---|
| `> 900px` | 地圖頁左右兩欄；地圖 sticky |
| `≤ 900px` | 地圖頁改為單欄堆疊；**sticky 取消**（堆疊後固定的地圖會擋掉大半畫面）；細節卡片取消固定高度 |
| `≤ 820px` | 收容所列表列改為兩欄折行，名稱與地址佔滿整行 |
| 動物卡片網格 | 一律 `auto-fill minmax(190px, 1fr)`，自動決定每列張數 |

---

## 11. 尚未實作、可一併設計的畫面

### 分析頁（規劃中）

預計包含這些模組，資料多半已備妥：

- **收容所量能四象限**：散點圖，X 軸在所數、Y 軸滯留中位數。右上角是「量大又出不去」的高壓收容所
- **縣市 × 滯留天數熱圖**：矩陣式，每個縣市一列、七個天數區間一欄
- **品種犬 vs 混種犬**：兩組對照，中位數 197 天 vs 711 天
- **存量殘存曲線**：按建檔年份的在所數，單調遞減
- **開放資料品質評分卡**：各縣市的絕育登錄率（97% 到 13%）、照片覆蓋率、尋獲地區級覆蓋率

**注意**：不會有「尋獲地熱區圖」。尋獲地欄位只有 0.1% 有完整地址，做出來的熱區只會反映各收容所的登錄習慣，不是動物實際被撿到的位置。

---

## 12. 給重新設計的幾點要求

1. **顏色一律引用 token，不要寫死 hex**，否則深色模式會失效
2. **深色模式的資料色階是反向的**，不能把淺色那組直接套用
3. **保留「這張圖不能拿來說什麼」區塊**，並維持與內容相當的視覺份量
4. **中文字串照抄**，包含「已在所」「在所數」「滯留中位數」這些用詞——它們是刻意選的，「在所」不等於「收容」，「已在所」不等於「等待認養」
5. **不要加 emoji、插畫或吉祥物**
6. **不要出現「立即認養」這類 CTA 按鈕**。本站不辦理認養，只呈現公開資料，導向行動會誤導
7. 數字欄位維持等寬數字對齊
