# StrayAtlas 設計說明

> 這份文件描述**目前已實作**的介面：畫面長什麼樣，以及為什麼是這樣。
> 它同時是改動時的約束清單（§12），不只是現況紀錄。
> 對應的程式碼在 `src/`，色彩 token 在 `src/style.css`。
> 語氣與用字以實際畫面上的文案為準，不要另行翻譯或改寫。
>
> 2026-09-17 依版面改版（`.notes/drafts/*.html`）全面改寫。章節編號 §3、§6、§11、§12
> 被程式註解引用，改動時請保留編號。

---

## 1. 這個網站在做什麼

臺灣公立動物收容所「目前仍開放認養」的動物名冊，資料來自農業部開放資料。每日排程保存來源快照；網站畫面則以某一份快照為準，日期標在頁尾（§4），**不宣稱每日更新**。

**三種使用者，看的是同一份資料但問不同的問題：**

| 使用者 | 想知道的事 | 對應畫面 |
|---|---|---|
| 想認養的人 | 「我家附近有哪些貓可以認養？這間收容所在哪、電話幾號？」 | 首頁、找動物、收容所 |
| 關心議題的人／記者／動保團體 | 「哪個縣市的動物待最久？哪間收容所壓力最大？」 | 縣市地圖、收容所 |
| 想看資料本身的人 | 「在所天數的分布長什麼樣？這個結論站得住嗎？」 | 資料分析、關於本站 |

農業部的寵物登記管理網站（pet.gov.tw）已有官方的動物認領養公告頁，本站用的是同一份開放資料，差別在讀法：把「已在所多久」算出來放到和照片一樣顯眼，並能跨收容所、跨縣市比較。

**貫穿全站的一個原則：主動說明資料的限制。** 每個資料頁底部都有一塊說明「這一頁／這張圖不能拿來說什麼」，分析頁則把警語卡放在圖表**上方**。這不是免責聲明，是內容的一部分——它是這個作品想展示的判斷力，版面上不應該把它縮小或藏起來。

---

## 2. 視覺調性

**溫暖、克制、資訊密度中等。** 動保題材需要親近感，但滯留天數這種冷硬數字放進過度活潑的版面會變成行銷頁。平衡點是：暖色底、圓角、膠囊按鈕、字距寬鬆，但資料呈現本身保持樸素。

- 單一強調色系（橘）。第二個彩色只用在兩個地方：貓的類別色（§3）與地圖圖釘（§3 `--pin`）
- 無陰影，卡片以 1px 細線分隔
- 無 emoji、無插畫、無漸層、無吉祥物；圖示一律 Lucide 線條圖示
- 首頁 hero 不放照片（2026-09-15 決定），動物照片只出現在動物卡片與詳細資料
- 中文字型 `Noto Sans TC`（`index.html` 載入），回退到系統 UI 字型
- **只有淺色**。深色模式與主題切換已於 2026-09-15 移除，`color-scheme` 固定為 `light`

---

## 3. 設計 token

**這些值請直接沿用。** 顏色一律引用 token，不要在元件裡寫 hex。

### 色彩

| 用途 | Token | 值 |
|---|---|---|
| 頁面底色 | `--plane` | `#f7f2ea` |
| 卡片／面板 | `--surface` | `#fdfaf6` |
| 凹陷區（警語卡、說明框、hero 底） | `--surface-sunk` | `#f2ebe1` |
| 主要文字 | `--ink` | `#1c1917` |
| 次要文字 | `--ink-secondary` | `#57534e` |
| 弱化文字（標籤、單位） | `--ink-muted` | `#8a8178` |
| 分隔線 | `--hairline` | `#e6ddd0` |
| 無資料 | `--no-data` | `#e9e2d6` |
| 橘色底上的文字與圖示 | `--on-accent` | `#fdfaf6` |

### 資料色階（單色相，六階，數值由小到大）

| 階 | 值 |
|---|---|
| `--ramp-1` | `#eda17a` |
| `--ramp-2` | `#e28654` |
| `--ramp-3` | `#d66a27` |
| `--ramp-4` | `#b85a21` |
| `--ramp-5` | `#984e24` |
| `--ramp-6` | `#794224` |

`--ramp-4` 兼作互動強調色：被選中的膠囊按鈕、主要按鈕、首頁的橘色色帶、選中的地圖圖釘。hover 邊框用 `--ramp-3`。

這組色階通過了對比度檢查：單一色相、亮度單調遞增、相鄰階亮度差 ≥ 0.06、最接近底色的那一階對底色仍有 2:1 對比。**若要換色，整組要重新驗證，不能只換其中幾階。**

白字（`--on-accent`）放在色塊上的對比：`--ramp-4` 4.46（略低於一般文字的 4.5），`--ramp-5` 5.85，`--ramp-6` 7.71。**大面積白字要放在橘色底上時，底色至少用 `--ramp-5`。**

### 補充色

| 用途 | Token | 值 |
|---|---|---|
| 內文中的數字強調 | `--accent-text` | `#984e24` |
| 類別色 A（狗） | `--series-a` | `#d66a27` |
| 類別色 B（貓） | `--series-b` | `#00799a` |
| 地圖上的收容所圖釘 | `--pin` | `#3d5a73` |

`--accent-text` 比 `--ramp-4` 深一階，因為它是**文字**，要通過文字對比度而不是色塊對比度。

`--series-a/b` **只在狗與貓被當成兩條並列的資料序列時使用**（分析頁 ECDF、地圖面板的狗貓比例條）。上面那組色階是序列型的，承載的是「大小」，無法表達「身分」；反過來也一樣，這兩個色不可拿來表示數量多寡。這對顏色經過驗證：亮度落在合理帶內、彩度過下限、protan／deutan 模擬下 ΔE 16.9、對底色有 3:1 對比。**要加第三個類別色不能用「再挑一個顏色」解決**，那在色覺缺陷下會與既有的撞在一起——該改小倍數圖或把尾巴收成「其他」。

`--pin`（石板藍，2026-09-16 選定）讓圖釘和橘色縣市色階分成兩層，也刻意避開 `--series-b`，免得圖釘被讀成「貓」。白字在上面約 7:1。

### 尺寸

| 項目 | 值 |
|---|---|
| 內容最大寬度（`.wrap`） | `1180px`，左右內距 `1.5rem` |
| 一般頁面上方留白 | `3rem`（首頁為滿版，自行排版） |
| 卡片圓角 `--radius` | `12px` |
| 小元件圓角 `--radius-sm`（下拉、清單列、說明框） | `8px` |
| 膠囊按鈕圓角 | `999px` |
| 基礎字級／行高 | `16px` / `1.7` |
| 頁面標題 h1 | `clamp(1.6rem, 3.4vw, 2.2rem)` |
| 區塊標題 h2 | 約 `1.15rem`–`1.35rem` |

數字一律使用 `font-variant-numeric: tabular-nums`，否則直向對齊會歪。

---

## 4. 版面骨架

所有頁面共用同一個外殼（`src/App.vue`）：

```
┌──────────────────────────────────────────────────────────────┐
│ StrayAtlas 浪浪地圖     找動物 收容所 縣市地圖 資料分析 關於本站 │  ← 頂欄，sticky，高 60px
├──────────────────────────────────────────────────────────────┤
│ 頁面標題                                                      │  ← PageHead：h1＋一段引言
│ 一段引言                                                      │     （首頁改用 hero）
│                                                              │
│                        ← 各頁內容 →                          │
│                                                              │
│ ┌ 這一頁不能拿來說什麼 ───────────────────────────────────┐   │  ← 邊界卡（§12.3）
│ └───────────────────────────────────────────────────────┘   │
├──────────────────────────────────────────────────────────────┤
│ 資料快照 2026-09-16  全站的名單與數字都以這一天的資料為準。     │  ← 頁尾
│ 資料來源：農業部「動物認領養」開放資料（dataset 85903）…        │
│ 本專案程式碼目前保留所有權利…介面圖示採用 Lucide（ISC License）。│
└──────────────────────────────────────────────────────────────┘
```

- **標題可點**，回到首頁 `/`
- 導覽為純文字連結，當前頁以墨色粗體標示（`aria-current="page"`）；`/shelters/:id` 也標在「收容所」
- **≤ 820px**：導覽收成右上角的選單按鈕，點開是由右滑入的全螢幕面板（每列一個連結＋右箭頭）。開啟時鎖住背景捲動、焦點移到關閉鈕；Esc 或關閉鈕關閉並把焦點還給選單按鈕；換頁或視窗放寬時自動關閉。`prefers-reduced-motion` 時不做滑動
- **資料快照日期全站只寫一次，在頁尾第一行**，讀 `public/data/meta.json`（約 4 KB，每頁都能顯示而不必載入該頁的大檔）。讀取失敗時整行不顯示。各頁說明需要提到日期時一律寫「以頁尾的快照日期為準」
- 頁尾的 pet.gov.tw、dataset 85903、Lucide 都是外部連結

### 路由

| 路徑 | 畫面 |
|---|---|
| `/` | 首頁（滿版，`meta.fullBleed`） |
| `/animals` | 找動物 |
| `/shelters` | 收容所列表 |
| `/shelters/:id` | 收容所介紹 |
| `/map` | 縣市地圖 |
| `/analysis` | 資料分析 |
| `/about` | 關於本站 |

其他路徑導回 `/`。GitHub Pages 以 `404.html` 回退到 SPA。

### 狀態放在網址

篩選、排序、分頁、檢視模式與開啟中的動物都寫進 query string，**網址就是狀態**：重新整理、分享連結、上一頁都能回到同一個畫面。預設值不寫進網址；不認得的值（包括本次快照裡已不存在的縣市、收容所、品種）直接忽略。

- 改篩選用 `router.replace`（不堆歷史），換頁與開啟動物用 `router.push`
- 捲動：上一頁／下一頁還原位置；只改 query 時不捲動；換路徑時回到頂端
- 各頁的參數見各節

---

## 5. 首頁（`/`）

**目的**：讓第一次來的人在一個畫面內知道「有哪些動物、怎麼找、怎麼認養」，並把等最久的動物放在最前面。

由上而下：

1. **Hero**：`--surface-sunk` 滿版色帶，中央一塊白底標題板。「牠們一直都在等待被看見」＋「全臺 37 間公立收容所目前仍開放認養的動物。」不放照片
2. **搜尋框**（`--ramp-4` 底）：「尋找狗狗／尋找貓咪」兩個分頁（附隻數）、搜尋欄「以縣市、收容所、品種搜尋」、快捷膠囊（該物種在所數前 4 的縣市＋「幼體」「小型」「已在所 2 年以上」，隻數為 0 者不顯示），以及「增加篩選條件」連到找動物
3. **三個數字**：在所隻數、收容所與縣市數、已在所天數中位數
4. **等最久的**：不分物種、已在所天數最長的 10 隻，橫向捲動一次一張，左右箭頭對齊照片中線。**這一區不放說明句**（2026-09-16 決定）
5. **最新建檔的狗狗／貓咪**：各 4 隻，依建檔日由新到舊，附「全部 N 隻 →」
6. **認養流程**：六步驟，分成「在 STRAYATLAS」與「在收容所受理」兩道泳道；以臺北市動物保護處公告為例，並註明各縣市規定不同。開頭就說「本站不辦理認養」
7. **從種類搜尋**（橘色色帶）：米克斯比例、犬種前 10／貓種前 8
8. **依條件瀏覽**（橘色色帶）：依縣市、依體型與年齡、依已在所天數三張卡
9. **關於這份資料**：三欄邊界卡（右設限／長度偏差抽樣／欄位覆蓋），右上連到資料分析頁

首頁所有連結都帶入找動物的 query（§6），不另做頁面。

---

## 6. 找動物（`/animals`）

**目的**：認養人視角。全國 8,000 餘隻，用篩選收斂到「我要的那幾隻」。狗、貓與其他動物在同一個清單裡。

### 篩選分兩層

```
縣市 [全部（8,336 隻）▾]   收容所 [全部（37 間）▾]
────────────────────────────────────────────────────────────  ← 分隔線
動物類型 (全部 n)(狗 n)(貓 n)(其他 n)   品種 [全部（n 種）▾]
性別 (不限)(公 n)(母 n)(未填 n)   體型 (不限)(小型 n)(中型 n)(大型 n)
已在所 (不限)(30 天內 n)(31–90 天 n)(91–365 天 n)(1–2 年 n)(2–5 年 n)(5 年以上 n)
```

**上層是地區，下層是動物本身的條件**，中間用一條細線分開。縣市與收容所連動——選了縣市，收容所下拉只剩該縣市的；換縣市會清掉收容所，換類型會清掉品種。

**每個選項旁的數字是分面計數**：套用其他所有條件、唯獨不含這一項本身時的隻數，所以點下去之前就知道會剩多少。

### 為什麼有的是膠囊按鈕有的是下拉

| 控制項 | 形式 | 理由 |
|---|---|---|
| 動物類型、性別、體型、已在所 | 膠囊按鈕 | 選項少、常切換、當前狀態不用打開就看得見 |
| 品種 | 下拉 | 長度不固定：某間收容所 11 種、另一間只有 2 種 |
| 縣市（22）／收容所（37） | 下拉 | 選項太多，攤開會佔掉整個畫面 |

**重新設計時請維持這個分界。** 把三個選項塞進下拉，使用者要三個動作才能完成一次本來一次點擊的事。

年齡（幼體／成體）沒有控制項，只從首頁連結帶入，以「已套用條件」標籤顯示。

### 結果列與已套用條件

「符合條件 **n** 隻」＋排序下拉（已在所天數長到短／短到長）。下方一排已套用條件標籤，每個都能單獨移除，最後是「清除全部」。

### 動物卡片網格

固定 **4 欄**，≤ 820px 為 **2 欄**；**每頁 16 隻**（4 與 2 都能整除，最後一列不會只剩零星幾張）。

```
      ┌───────────┐
 [已在所 4,561 天]│    ← 黑底徽章疊在照片左上
      │   照片    │    ← 正圓；無照片「無照片」，載入失敗「照片無法載入」
      └───────────┘
      ♀ 混種犬         ← 性別圖示＋品種（空白為「未填品種」）
    臺北市動物之家      ← 收容所
  ━━━━━━━━━━━━━━━━━━  ← 排名條，寬度＝等得比多少比例的動物久
   等得比 99.9% 的動物久
  (   查看詳情    →  )
```

- 整張卡可點，開啟動物詳細資料（§10）
- 全臺等最久的那一隻，排名條下的字改成「牠是全臺等最久的那一隻」
- hover 時只放大照片，外框不動

### 分頁

數字頁碼＋上一頁／下一頁。總頁數 ≤ 7 全部列出，否則保留首末頁與當前頁前後各一，其餘以「…」省略。換頁平滑捲到結果區頂端。頁碼下方「顯示第 a–b 隻，共 n 隻 · 每頁 16 隻」。

### 邊界卡

標題「這些卡片不能拿來說什麼」，三欄：刊登照片不代表動物現況／「已在所天數」不是難認養程度／本站不辦理認養。

### 網址參數

`kind=dog|cat|other`、`county`、`shelter`、`variety`、`sex=M|F|N`、`body=SMALL|MEDIUM|BIG`、`age=CHILD|ADULT`、`days=0-30|31-90|91-365|1-2y|2-5y|5y+`、`daysFrom=<同上>`（該區間以上）、`q`（多個詞須全部出現在縣市、收容所、品種之中）、`sort=shortest`、`page`、`animal`。

---

## 7. 收容所（`/shelters`、`/shelters/:id`）

### 列表頁

頁首三個數字（收容所數、縣市數、在所隻數），下方篩選：縣市（下拉）、收容所名稱（文字）、目前收容（有狗／有貓）、規模（300 隻以上／100–299／100 以下）。排序七種：在所動物數多到少／少到多、滯留中位數、等最久的一隻、狗的數量、貓的數量、縣市由北到南。

結果列右側有**顯示方式切換**：純圖示的「卡片」（layout-grid）與「條列」（list），按下的一顆填 `--ramp-4`。

- **卡片**：3 欄（≤ 1000px 2 欄、≤ 640px 1 欄）。縣市膠囊、名稱、地址與電話、在所數與狗／貓／其他、在所時間分布小圖、「收容所介紹」按鈕與「看這裡的 N 隻動物 →」
- **條列**：同樣的內容排成滿版寬列，由左到右是名稱與聯絡、四個數字、分布小圖、兩個動作。≤ 1000px 名稱佔滿第一行，≤ 640px 全部直排

**地址全部列出**：來源資料登錄了多個地點的收容所全部列出；只是寫法不同（`~` 與 `-`、全形與半形）的合併為一筆；地址欄混入的地圖連結或座標不顯示，座標留給介紹頁定位。電話位數不足時標示「電話欄位不完整」，不做撥號連結。

網址參數：`county`、`q`、`has=dog|cat`、`size=big|mid|small`、`sort`、`view=list`。

### 收容所介紹頁

**沒有做「縣市 → 收容所」的下鑽**，列表直接連到每一間的介紹頁。頁首是名稱與一段引言（全臺在所數第幾多、出發前請先來電），不放縣市膠囊。

下方三個區塊，標題前各有一個淺底方框圖示，標題本身無框無底色、不加編號，區塊之間細線分隔：

1. **這裡等最久的**（hourglass）：已在所最久的 4 隻＋「看這裡的全部 N 隻動物」「只看狗」「只看貓」「已在所 2 年以上」
2. **聯絡與位置**（map-pinned）：左側地址與電話（手機可點擊撥號）＋「本站不辦理認養」說明框；右側 Google 地圖
3. **收容動物現況**（chart-column）：左側數字卡（在所總數放大、狗、貓、滯留中位數附全臺對照、最長滯留），右側在所時間分布圖（全部／狗／貓切換）

**地圖**用 Google Maps Embed API（place 模式，不限量免費，但需要 API key）。key 由建置時的 `VITE_GOOGLE_MAPS_EMBED_KEY` 注入，部署讀 repository secret `GOOGLE_MAPS_EMBED_KEY`，必須在 Google Cloud 限制網站來源。**沒有 key 時**地圖區顯示「點下方連結，在 Google 地圖查看這間收容所的位置。」，下方保留「在 Google 地圖開啟」連結。

---

## 8. 縣市地圖（`/map`）

**目的**：一眼看出動物集中在哪些縣市、哪些縣市待得特別久，並且能一路點到單一收容所。

### 版面

```
[顯示指標: 在所數|滯留中位數] [動物類型: 全部 n|狗 n|貓 n] [圖層: 收容所圖釘 37]

┌───────────────┬──────────────────────────────────────────┐
│ 全臺           │ ┌─────┐                    點選縣市或圖釘 │
│ 全臺公立收容所  │ │連江縣│          ╭─╮                     │
│ [8,336][37]    │ └─────┘         ╭╯ ╰╮                    │
│ [351天][4,6xx] │ ┌─────┐        ╭╯   ╰╮                   │
│ ▬▬▬▬▬▬▬▬       │ │金門縣│        │     │                   │
│ 狗 n    貓 n   │ └─────┘        ╰╮   ╭╯                   │
│ 在所數最多的    │ ┌─────┐          ╰─╮╭╯                   │
│ 六個縣市        │ │澎湖縣│            ╰╯               [+]  │
│ …              │ └─────┘                             [−]  │
├───────────────┴──────────────────────────────────────────┤
│ 在所動物數（隻） ▬▬▬▬▬▬ 1–49 … 1,000 以上   ▭ 0 隻／沒有公立收容所 │
└──────────────────────────────────────────────────────────┘
┌ 這張圖不能拿來說什麼 ─────────────────────────────────────┐
└──────────────────────────────────────────────────────────┘
```

控制框、地圖卡（左面板＋右地圖，下方圖例帶）、邊界卡。

### 左側面板：常駐，三種狀態

| 狀態 | 內容 | ✕ |
|---|---|---|
| 全臺 | 四個數字（在所、收容所數、中位數、最長）、狗貓比例條、依目前指標排前六的縣市 | 無 |
| 縣市 | 同樣四個數字與比例條、這個縣市的收容所清單 | 回到全臺 |
| 收容所 | 在所、中位數、平均、最長、比例條、地址與電話、「收容所介紹」按鈕與「看這裡的 N 隻動物 →」 | 回到該縣市 |

- 點縣市開縣市狀態；**再點一次同一個縣市回到全臺**
- 點圖釘或清單裡的收容所開收容所狀態，並同時標記它所在的縣市
- 「再點一次＝關閉」的判斷放在頁面層，不在地圖元件裡：圖釘點擊也會設定縣市，地圖元件分不出是哪一種狀態造成的
- 面板高度跟著地圖，清單過長時在面板內捲動（`contain: size`）

### 地圖細節

- **SVG 向量圖，沒有底圖圖磚**。22 個縣市多邊形，最大高度 720px（以 viewBox 比例換算寬度，滑鼠座標換算才不會錯）
- 連江、金門、澎湖畫成**左側三個插圖框**，只標縣名全名。它們離本島太遠，同框會把本島壓成拇指大
  - 插圖畫在最後一層且底色不透明，**不隨主圖縮放**，任何倍率下都看得見
  - 左側 112 單位是插圖的保留區，主圖被裁切在保留區之外——插圖在地圖**旁邊**，不是蓋在地圖上
- 縣市之間以 `--surface` 色 1.4px 描邊隔開
- **hover**：滑到的縣市保持原色，其餘降到 30% 不透明度
- 開啟圖釘圖層時，縣市底色整體降到 45%

### 色階：固定門檻

| 指標 | 六階 |
|---|---|
| 在所數（隻） | 1–49、50–99、100–199、200–499、500–999、1,000 以上 |
| 滯留中位數 | 90 天內、90–179 天、180–364 天、1–2 年、2–4 年、4 年以上 |

**不用分位數分級**（2026-09-16 改）：分位數每次切換狗／貓都會重算區間，兩張圖的顏色就不能互比。固定門檻下同一個顏色在任何檢視都代表同一個數字。0 隻畫成無資料色；「滯留中位數」下在所不足 20 隻的縣市也畫成無資料，圖例會註明。

### 收容所圖釘

- 圖釘圖層**預設關閉**
- 形狀是**地標圖釘**，尖端才是位置
- **面積**正比於在所數（半徑取平方根），數字印在釘頭裡
- 顏色是單一 `--pin`，不隨數量變化。開啟面板的那一間改用 `--ramp-4`，並畫在最上層
- 名稱只在鏡頭夠近（k ≥ 2.5）時顯示，按在所數由大到小排版，會碰到別的圖釘或名稱就放棄
- 位置是**行政區形心，不是門牌**，頁面上必須寫明

### 縮放與平移

- 滾輪縮放（以游標為錨點）、放大後拖曳平移、右下角＋／−（Lucide 圖示）
- **沒有「回到全臺」按鈕**：兩顆按鈕都以畫面中心為錨點，連按 − 就會回到 1 倍
- 單擊縣市＝開面板，**連點兩下**才把鏡頭框到該縣市
- 拖曳結束時不觸發縣市選取

### 邊界卡

「這張圖不能拿來說什麼」六點：收容所的地圖不是流浪動物的地圖（尋獲地只有 2.4% 寫出縣市，其中 6% 與收容縣市不同）／顏色混著動物量與收容所數／只算得出目前仍在所的動物（中位數對平均的差距）／在所數是存量不是流量／圖釘是行政區形心不是門牌／切換狗貓時門檻不變、灰色的意義。

網址參數：`metric=median`、`kind=dog|cat`、`pins=1`、`county=<縣市>`、`shelter=<id>`（有 `shelter` 時不寫 `county`）。

---

## 9. 關於本站（`/about`）

**目的**：回答「這個站是什麼、資料從哪來、多新、為什麼這樣做」。留在頂欄導覽。

1. **服務目的**：先說明官方已有 pet.gov.tw 的動物認領養公告頁、本站差別在讀法；目前仍在所的動物有多少比例已在所超過四年（由資料計算）；本站不辦理認養
2. **資料來源**：表格（動物資料、行政區界線、動物照片、認養流程、介面圖示、本站原始碼）
3. **更新頻率**：兩格（這份快照的動物與收容所數、「不定期」），說明畫面以頁尾快照日期為準、不會跟著來源自動更新，要看最新名單請到官方公告頁或致電收容所；每日排程保存的原始快照是留給日後分析用的
4. **技術選擇**：每一列寫「為什麼這樣選」，不是版本號
5. **免責聲明**：`--surface-sunk` 底的條列

**刻意不放**：統計陷阱與尋獲地熱區圖兩段（2026-09-16 決定，這些留在 README）。不署名、不放 email，只放 GitHub repo 連結。

---

## 10. 動物詳細資料、狀態與響應式

### 動物詳細資料（Modal）

點卡片開啟。**不是換頁**，因為使用者可能已經設好篩選、翻到第幾頁；換頁再返回會全部失去。但網址會同步變成 `?animal=<id>`，所以連結可分享、上一頁關閉 modal（若是從分享連結直接進來，關閉時改為移除參數，不會跳出網站）。

```
┌──────────────────────────────────────────┐
│ 收容編號 #…   [公立收容所資料快照]      [×] │  ← 黏頂
│                                          │
│            照片 3:2（contain）            │  ← 完整顯示不裁切；左上「在所 x 年 · 影像原圖無裁切」
│                                          │     無照片或載入失敗時縮成窄帶
├──────────────────────────────────────────┤
│ 臺北市 · 臺北市動物之家  (02)…            │
│ 混種犬                     已在所 4,561 天 │
│                              （約 12.5 年）│
│ 規格化資料欄位                              │
│ 收容編號 流水號 性別 體型 年齡 毛色          │
│ 絕育狀態 開放認養日 建檔日                  │
│ 收容所備註                                 │
│ ※ 欄位內容依各收容所登錄實務而異…           │
│ [複製此動物頁面連結]               [關閉]   │
└──────────────────────────────────────────┘
```

寬 `min(720px, 100%)`、最大高 `min(90vh, 860px)`，內部可捲。背景遮罩 `rgba(20,16,12,0.55)`，點遮罩、按 Esc、按 × 或「關閉」都會關閉，開啟時鎖住背景捲動。

### 狀態

每個資料驅動的畫面都有對應樣式：

| 狀態 | 呈現 |
|---|---|
| 載入中 | 純文字「載入中…」，弱化色。找動物與首頁另註明「（全國動物資料約 272 KB）」 |
| 載入失敗 | 「資料載入失敗（錯誤訊息）。」；地圖頁另有「重試」 |
| 篩選後為空 | 「沒有符合條件的動物。」／「沒有符合條件的收容所，試著放寬條件。」 |
| 收容所不存在 | 「找不到這間收容所，它可能已不在最新的資料裡。」＋回列表連結 |
| 收容所圖釘載入失敗 | 圖釘圖層不出現，choropleth 照常。圖釘是疊層，不該把地圖一起拖下水 |
| 輔助資料載入失敗 | 頁尾快照日期、分析頁與關於本站引用的單句數字：只省略那一行，不報錯 |
| 地圖沒有 API key | 見 §7 |

### 響應式

| 斷點 | 變化 |
|---|---|
| `≤ 1080px` | 首頁認養流程改直排 |
| `≤ 1000px` | 收容所卡片 3→2 欄、條列拆兩行；介紹頁聯絡區與現況區改單欄；首頁「依條件瀏覽」3→2 欄；各頁三欄邊界卡改單欄 |
| `≤ 900px` | 地圖頁改單欄，**地圖與圖例排在面板上方**（面板在上時，點地圖改變的是已捲過去的文字）；分析頁警語卡改單欄、排行列隱藏間數 |
| `≤ 820px` | 頂欄收成選單；找動物與首頁網格 4→2 欄；篩選欄位與下拉改滿寬；介紹頁動物 4→2 欄 |
| `≤ 700px` | 關於本站更新頻率兩格改直排 |
| `≤ 640px` | 收容所列表條列與卡片改單欄；介紹頁數字卡兩欄、在所總數佔一整列 |
| `≤ 520px` | 各頁面板內距縮小、欄位滿寬；收容所列表切換鈕與排序同一行 |
| `≤ 480px` | 首頁搜尋分頁隱藏隻數；分頁按鈕縮為 36px |

分析頁兩張圖在手機上保持最小寬度 560px、改為左右捲動，不把刻度字縮到讀不到。

---

## 11. 資料分析（`/analysis`）

**目的**：回答地圖答不了的那個問題——在所天數的分布長什麼樣。

版面是單欄堆疊的卡片，由上而下：

1. **警語卡**（`--surface-sunk` 底）。**放在圖表上方，不是註腳**——這頁只有在這段先被讀到時才是誠實的
   - 左上為 triangle-alert 圖示加「方法學指引與資料邊界」膠囊、右為 `DATA BOUNDARY & BIAS`
   - 三欄，各有編號 01／02／03、中英並列的術語（右設限 Right-Censoring／長度偏差抽樣 Length-Biased Sampling／存活分析 Survival Analysis），以及底部一行狀態標籤。底部用 `margin-top: auto` 對齊
   - 排成直式編號清單時第三點會被讀成補充；三欄之後三點份量相等
2. **控制卡**：對象（全部／狗／貓）、橫軸（對數／線性）、平滑程度（較細／標準／較平滑），右端是**已套用**的摘要，含當前頻寬數值。頻寬是唯一「不講出來就看不出影響」的設定
3. **在所天數的分布**：長條（`--ramp-2`，滑到的一根轉 `--ramp-4`）與核密度曲線（`--ramp-5`）共用同一條 y 軸（兩者都是密度），中位數以墨色虛線標示，近右緣時標籤翻到左側。刻度寫「1 週」「1 個月」「10 年」，線性軸以年為單位。圖下方有圖例與滑過讀數
4. **累積分布（ECDF）**：狗與貓兩條，用 `--series-a/b`，**橫軸跟著上面的對數／線性設定**。畫成**階梯**不是折線——ECDF 在每個觀測點跳躍，兩隻動物之間畫斜線等於宣稱沒人有過的數值。1 年與 4 年兩條參考虛線，對應說明文字引用的門檻
5. **分位數表**：隻數、P25、中位數、P75、P90、最長、超過 1 年、超過 4 年。讓每個數字可以被複製查證，也是圖表之外的替代讀法
6. **各縣市**：純文字列（序號、縣市、間數、隻數、中位數），依在所數排序。中位數達全國兩倍者用強調色粗體

每張圖下方的說明都有**自己的小標**與凹陷底色（數據判讀與尺度邊界／階梯曲線判讀指引／偏態分布判讀重點／縣市排行資料背景與口徑差異）。**說明裡的數字全部由資料計算**（平均與中位數的倍數、兩倍門檻與達標縣市數、間數範圍、尋獲地筆數），換快照時不會過期。

網址參數：`kind=dog|cat`、`axis=linear`、`smooth=fine|smooth`。

### 這頁的規矩

- **頻寬要公開**。KDE 的形狀有一半是頻寬的主張，只給一條曲線等於把選擇藏起來。三段頻寬是 Silverman 值的 0.6／1.0／1.7 倍
- **可引用的數字讀 ECDF，不讀 KDE**。ECDF 沒有頻寬也沒有平滑假設，「六成的狗已在所超過一年」不該取決於一個讀者可以拖動的控制項
- **兩種軸都給**。線性軸下密度是單調遞減的長尾；對數軸下才看得到兩個隆起。只放好看的那個等於替讀者做了他看不見的選擇
- **門檻要寫出來**。排行的強調色門檻（中位數達全國兩倍）寫在說明裡：寫出來的門檻讀者可以不同意，沒寫的門檻就變成對該縣市的指控
- **用詞只談「已在所」**。不寫「多久會離開」「多快被認養」，這份資料量不到

### 明確不做的圖

**尋獲地熱區圖。** 尋獲地欄位只有 0.1% 有完整地址，區級覆蓋率各縣市從 0% 到 95.1% 不等。做出來的熱區反映的是各收容所的登錄習慣，不是動物實際被撿到的位置。

---

## 12. 設計約束

改動任何畫面時，以下各條先於美觀：

1. **顏色一律引用 token，不要寫死 hex**
2. **只有淺色**。要恢復深色模式，必須另選一組值並重新驗證資料色階（深色底上色階要反向），不能把淺色反轉
3. **保留「這一頁／這張圖不能拿來說什麼」與分析頁的警語卡**，並維持與內容相當的視覺份量。這是這個專案最有說服力的部分，不是可以裁掉的裝飾
4. **中文字串照抄**，包含「已在所」「在所數」「滯留中位數」「目前仍在所」這些用詞——它們是刻意選的，「在所」不等於「收容」，「已在所」不等於「等待認養」
5. **不要加 emoji、插畫或吉祥物**
6. **不要出現「立即認養」這類 CTA 按鈕**。本站不辦理認養，只呈現公開資料，導向行動會誤導
7. 數字欄位維持等寬數字對齊（`font-variant-numeric: tabular-nums`）
8. **資料快照日期只寫在頁尾**，各頁不重複標示；**不宣稱「每日更新」**，除非排程真的每天重建並部署網站資料
9. **說明文字裡的數字由資料計算**，不要把某一天快照的數字寫死在元件裡
