# StrayAtlas 設計說明

> 這份文件描述**目前已實作**的介面：畫面長什麼樣，以及為什麼是這樣。
> 它同時是改動時的約束清單（§12），不只是現況紀錄。
> 對應的程式碼在 `src/`，色彩 token 在 `src/style.css`。
> 語氣與用字以實際畫面上的文案為準，不要另行翻譯或改寫。

---

## 1. 這個網站在做什麼

臺灣公立動物收容所「目前仍開放認養」的動物名冊，資料來自農業部開放資料，每日自動更新。

**兩種使用者，看的是同一份資料但問不同的問題：**

| 使用者 | 想知道的事 | 對應畫面 |
|---|---|---|
| 想認養的人 | 「我家附近有哪些貓可以認養？」 | 找動物、收容所 |
| 關心議題的人／記者／動保團體 | 「哪個縣市的動物待最久？哪間收容所壓力最大？」 | 縣市地圖 |
| 想看資料本身的人 | 「在所天數的分布長什麼樣？這個結論站得住嗎？」 | 分析 |

**貫穿全站的一個原則：主動說明資料的限制。** 每個畫面底部都有一塊說明「這張圖不能拿來說什麼」。這不是免責聲明，是內容的一部分——它是這個作品想展示的判斷力，版面上不應該把它縮小或藏起來。

---

## 2. 視覺調性

**溫暖、克制、資訊密度中等。** 動保題材需要親近感，但滯留天數這種冷硬數字放進過度活潑的版面會變成行銷頁。目前的平衡點是：暖色底、圓角、字距寬鬆，但資料呈現本身保持樸素。

- 單一強調色系（橘），沒有第二個彩色
- 無陰影，卡片以 1px 細線分隔
- 無 emoji、無插畫、無漸層
- 中文字型優先 `Noto Sans TC`，回退到系統 UI 字型

---

## 3. 設計 token

**這些值請直接沿用。** 深色模式不是把淺色反轉，是各自選過的一組值，而且橘色階在深色模式下是反向的（深色底上，數值越大顏色越亮）。

### 色彩

| 用途 | Token | 淺色 | 深色 |
|---|---|---|---|
| 頁面底色 | `--plane` | `#f7f2ea` | `#12110f` |
| 卡片／面板 | `--surface` | `#fdfaf6` | `#1a1917` |
| 凹陷區（bar 底槽、hover） | `--surface-sunk` | `#f2ebe1` | `#23211e` |
| 主要文字 | `--ink` | `#1c1917` | `#faf7f2` |
| 次要文字 | `--ink-secondary` | `#57534e` | `#c9c1b6` |
| 弱化文字（標籤、單位） | `--ink-muted` | `#8a8178` | `#948b80` |
| 分隔線 | `--hairline` | `#e6ddd0` | `#332f2a` |
| 無資料 | `--no-data` | `#e9e2d6` | `#2b2824` |

### 資料色階（單色相，六階，數值由小到大）

| 階 | 淺色 | 深色 |
|---|---|---|
| 1 | `#eda17a` | `#824624` |
| 2 | `#e28654` | `#a25223` |
| 3 | `#d66a27` | `#c35e20` |
| 4 | `#b85a21` | `#da7338` |
| 5 | `#984e24` | `#e69062` |
| 6 | `#794224` | `#f1ab88` |

`--ramp-4` 兼作互動強調色：被選中的 chip 底色、bar 填色、tab 底線、focus 外框。

這組色階通過了對比度檢查：單一色相、亮度單調遞增、相鄰階亮度差 ≥ 0.06、最接近底色的那一階對底色仍有 2:1 對比。**若要換色，整組要重新驗證，不能只換其中幾階。**

### 兩個補充色

| 用途 | Token | 淺色 | 深色 |
|---|---|---|---|
| 內文中的數字強調 | `--accent-text` | `#984e24` | `#e69062` |
| 類別色 A（狗） | `--series-a` | `#d66a27` | `#da7338` |
| 類別色 B（貓） | `--series-b` | `#00799a` | `#2b99bf` |

`--accent-text` 比 `--ramp-4` 深一階，因為它是**文字**，要通過文字對比度而不是色塊對比度。

`--series-a/b` **只在狗與貓被當成兩條並列的資料序列時使用**（目前只有分析頁的 ECDF）。上面那組色階是序列型的，承載的是「大小」，無法表達「身分」；反過來也一樣，這兩個色不可拿來表示數量多寡。這對顏色經過驗證：亮度落在合理帶內、彩度過下限、protan／deutan 模擬下 ΔE 16.9、對明暗兩個底色都有 3:1 對比。**要加第三個類別色不能用「再挑一個顏色」解決**，那在色覺缺陷下會與既有的撞在一起——該改用小倍數圖或把尾巴收成「其他」。

### 尺寸

| 項目 | 值 |
|---|---|
| 內容最大寬度 | `1180px` |
| 頁面內距 | 上 `3rem`／左右 `1.5rem`／下 `4rem` |
| 卡片圓角 | `12px` |
| 小元件圓角（select、清單列） | `8px` |
| chip 圓角 | `999px`（膠囊） |
| 卡片內距 | `1.25rem` |
| 基礎字級／行高 | `16px` / `1.7` |
| 標題 h1 | `clamp(1.9rem, 4vw, 2.6rem)` |

數字一律使用 `font-variant-numeric: tabular-nums`，否則直向對齊會歪。

---

## 4. 版面骨架

四個頁面共用同一個外殼：

```
┌──────────────────────────────────────────────────────┐
│  StrayAtlas 浪浪地圖              [系統][淺色][深色]  │  ← masthead
│  臺灣公立動物收容所目前仍開放認養的動物。 [資料快照 …] │
├──────────────────────────────────────────────────────┤
│  縣市地圖 │ 找動物 │ 收容所 │ 分析                       │  ← tabs（底線標示當前）
├──────────────────────────────────────────────────────┤
│                                                      │
│                    ← 各頁內容 →                       │
│                                                      │
├──────────────────────────────────────────────────────┤
│  資料來源：農業部「動物認領養」開放資料（dataset …）    │  ← footer
│  本專案程式碼目前保留所有權利…                          │
└──────────────────────────────────────────────────────┘
```

- **標題可點**，回到縣市地圖
- **資料快照日期**是一個膠囊標籤，緊接在副標之後，弱化色
- **主題切換**三段式（系統／淺色／深色），靠右，比一般 chip 小一號
- tabs 為底線式，非膠囊，與篩選用的 chip 明確區分

---

## 5. 畫面一：縣市地圖（`/`）

**目的**：一眼看出動物集中在哪些縣市、哪些縣市待得特別久，並且能一路點到單一收容所。

### 版面

**單欄，地圖佔滿整列。** 原本右側有一欄縣市排行，已移到分析頁——地圖上每一個縣市的顏色已經說了同一件事，把它再排成一張表只是重複，還跟地圖搶寬度。

地圖不 sticky。全寬之後它本來就佔掉大半個視窗，再釘住只會蓋住捲過去的內容。

```
[顯示指標: 在所數 | 滯留中位數]  [動物類型: 全部|狗|貓]  [圖層: 收容所圖釘]

┌──────────────────────────────────────────────────────────┐
│ 臺灣縣市分佈地圖                          點選縣市或圖釘   │
│ ┌────────────┐ ┌───┐                                     │
│ │ 新北市   ✕ │ │連江│           ╭─╮                      │
│ │ 9 間公立…  │ └───┘          ╭╯ ╰╮   ⬤725              │
│ │ 2,675 隻   │ ┌───┐         ╭╯   ╰╮  ⬤978              │
│ │ 257 天     │ │金門│         │臺灣 │                     │
│ │ ────────── │ └───┘         ╰╮   ╭╯                     │
│ │ 所內收容所 │ ┌───┐           ╰─╮╭╯                     │
│ │ 五股 725   │ │澎湖│             ╰╯                      │
│ │ 新店 568   │ └───┘                              ┌─┐    │
│ │ …          │                                    │+│    │
│ └────────────┘                                    │−│    │
│                                                    └─┘    │
│ 圖釘標示 37 處公立收容所…（位置為行政區形心而非門牌）      │
│ 在所數  ■<65 ■65-148 ■…                                  │
└──────────────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────────┐
│ 這張圖不能拿來說什麼                                       │
└──────────────────────────────────────────────────────────┘
```

### 地圖細節

- **SVG 向量圖，沒有底圖圖磚**。整張地圖是 22 個縣市多邊形
- 連江、金門、澎湖畫成**左上角三個插圖框**（細線框＋不透明底＋左下角標籤）。它們離本島太遠，同框會把本島壓成拇指大
  - 插圖畫在最後一層且底色不透明：地圖放大後本島會填滿畫面，畫在下層會被蓋掉
  - 插圖**不隨主圖縮放**，任何倍率下都看得見
  - **左側 112px 是插圖的保留區，主圖被裁切在保留區之外**。1 倍時本來就不重疊（主投影從 x=140 起算），但放大後本島會往左長到插圖底下——插圖是放在地圖**旁邊**，不是蓋在地圖上
- 縣市之間以底色描邊隔開（1.4px，`--surface` 色），視覺上像有縫隙
- **hover**：滑到的縣市保持原色，其餘降到 30% 不透明度
- 開啟圖釘圖層時，縣市底色整體降到 45%——兩組同色相的序列色階同時全彩會互相抵消，誰都讀不出來

### 收容所圖釘

- 形狀是**地標圖釘**，尖端才是位置，不是圓心
- **面積**正比於在所數（半徑取平方根），數字直接印在釘頭裡
- 顏色是單一墨色（`--ink`），不隨數量變化。數量已經由大小和數字表達；而且色階在深色模式下會反轉，用色階染色會出現「淺底白字」讀不到的情形
- 名稱只在鏡頭夠近（k ≥ 2.5）時顯示，並且按在所數由大到小排版，只要框會碰到別的圖釘或已放好的名稱就整個放棄。寧可少一個名稱，不要兩個名稱疊印
- 位置是**行政區形心**，頁面上必須寫明「不是門牌」

### 縮放與平移

- 滾輪縮放（以游標為錨點）、拖曳平移、右下角 ＋／−
- **沒有「回到全臺」按鈕**：兩顆按鈕都以畫面中心為錨點，連按 − 就會回到 1 倍
- 單擊縣市＝開面板，**連點兩下**才把鏡頭框到該縣市。單擊若同時框住縣市，鄰縣就會跑出畫面外，等於第一次點擊就剝奪了換一個縣市的能力
- 拖曳結束時不觸發縣市選取

### 左側面板（rail）

一個面板，四種狀態，從左緣滑入：

| 狀態 | 內容 |
|---|---|
| 收起 | 只有一顆「全臺收容所在所數」按鈕 |
| 清單 | 37 處收容所，可切由多到少／由少到多 |
| 縣市 | 該縣市統計＋該縣市的收容所清單 |
| 收容所 | 單一收容所的在所數、中位數、平均、最長、犬貓組成、地址、電話，以及進入該所動物列表的按鈕 |

- **面板開啟時地圖往右讓位**（`padding-left` 過渡 220ms）。地圖在全寬元素裡是置中留白的，這個位移吃掉的是原本就空著的左側空間，地圖本身不會縮小
- 「再點一次＝關閉」的判斷放在頁面層，不在地圖元件裡。地圖只知道哪個縣市被標記，不知道是四種狀態裡的哪一種造成的——圖釘點擊也會設定縣市，若由地圖自行判斷，收容所卡片開著時點該縣市會被誤判成重複點擊而收合

### 圖例

六個色塊＋數值區間，橫排。指標切換時區間會重算（分位數分級，非等距）。

---

## 6. 畫面二：找動物（`/animals`）

**目的**：認養人視角。全國 8,265 隻，用篩選收斂到「我要的那幾隻」。

### 篩選分兩層

```
縣市 [全部（8,265 隻）▾]   收容所 [全部（37 間）▾]
────────────────────────────────────────────────────  ← 分隔線
動物類型 [全部][狗][貓]   品種 [全部（8,265 隻）▾]   已在所 [不限][30天內][31-90天][91-365天][1-2年][2年以上]

符合條件 8,265 隻，依已在所天數由長到短排列。
```

**上層是地區，下層是動物本身的條件**，中間用一條細線分開。縣市與收容所連動——選了縣市，收容所下拉只剩該縣市的。

### 為什麼有的是 chip 有的是下拉

| 控制項 | 形式 | 理由 |
|---|---|---|
| 動物類型（3 項） | chip | 選項少、常切換、當前狀態不用打開就看得見 |
| 已在所（6 項） | chip | 同上 |
| 品種 | 下拉 | 長度不固定：某間收容所 11 種、另一間只有 2 種 |
| 縣市（22）／收容所（37） | 下拉 | 選項太多，攤開會佔掉整個畫面 |

**重新設計時請維持這個分界。** 把三個選項塞進下拉，使用者要三個動作才能完成一次本來一次點擊的事。

### 動物卡片網格

每列固定 1／2／4／5 欄（見 §10），間距 `1rem`，**同列等高**。

**為什麼不用 `auto-fill`。** `auto-fill` 會依可用寬度自行決定欄數，在某些寬度下算出 3 欄——而一頁 20 筆不能被 3 整除，最後一列只剩 2 張卡，看起來像載入失敗而不是這一頁結束了。所以欄數改為斷點寫死，只允許能整除 20 的 1／2／4／5，跳過 3。代價是 740px 以下直接從 4 欄掉到 2 欄，卡片會忽然變寬。

```
┌──────────────────────┐
│ [在所 12.5 年]        │  ← 徽章疊在照片左上，年數只出現在這裡
│      照片 4:3         │  ← object-fit: cover；無照片時顯示「無照片」
│                      │     載入失敗時顯示「照片無法載入」（兩者要能區分）
├──────────────────────┤
│ 混種犬    #103030912  │  ← 品種粗體；收容編號最弱化，靠右
│ 母·中型·成體·黑色      │  ← 次要色，中點分隔
│ ──────────────────── │
│ 已在所 4,561 天        │  ← 只給日數；年數由徽章負責，兩邊都寫會折行
│ 新北市中和區…          │  ← 僅「找動物」頁顯示，收容所頁省略
│ (02)86685547         │
└──────────────────────┘
```

品種與收容編號同一行，但那一行可以折行：收容編號最長 17 個字元且不斷字，四欄時會把品種擠成一行一個字，所以放不下時讓編號掉到下一行。

hover 時上移 2px、邊框轉為 `--ramp-3`。

### 捲動

捲到接近底部自動載入下 48 筆。底部同時保留「再顯示 48 隻（還有 N 隻）」按鈕作為備援，全部顯示完畢後改為「已顯示全部 N 隻」。

---

## 7. 畫面三：收容所（`/shelters`、`/shelters/:id`）

### 列表頁

37 間攤平在同一層，可依縣市篩選（此處用 chip 橫排，因為那份清單本身就是「全臺有哪些縣市」的資訊）。**沒有做「縣市 → 收容所」的下鑽**，因為 14 個縣市只有一間收容所，那一層對它們是空的點擊。

每列是一張可點的寬卡片：

```
┌────────────────────────────────────────────────────────────┐
│ 新北市                                                       │
│ 新北市五股區公立動物之家       725      248      狗 329       │
│ 新北市五股區成泰路…            在所    天中位數   貓 396      │
│                              ████▁▁▁                       │
└────────────────────────────────────────────────────────────┘
```

在所數附一條相對長度的細 bar，讓規模差異一眼可見（最大 2,675 對最小 6）。

### 單一收容所頁

頂部資訊卡（左側名稱／地址／電話，右側四個數字），下方是與「找動物」完全相同的篩選與卡片網格，但少了縣市與收容所兩個下拉。

**地址可能有多行**——三間收容所的來源資料登錄了兩種地址寫法，其中新北市瑞芳區是真的兩個地點。全部列出，並加一行說明，不擇一顯示。

---

## 8. 動物詳細資料（Modal）

點卡片開啟。**不是換頁**，因為使用者可能已經捲過幾百張卡片並設好篩選；換頁再返回會全部失去。但網址會同步變成 `?animal=<id>`，所以連結仍可分享、返回鍵關閉 modal。

```
┌──────────────────────────────────────┐
│                                  [×] │
│                                      │
│          照片 3:2（contain）          │  ← 完整顯示不裁切
│                                      │     無照片時此區塊縮成一條窄帶
├──────────────────────────────────────┤
│ 臺北市 · 臺北市動物之家                │
│ 混種犬                                │
│                                      │
│ 收容編號    流水號     性別            │
│ 103030912  31212     母              │
│ 體型        年齡      毛色            │
│ 中型        成體      黑色            │
│ 絕育        開放認養日  建檔日          │
│ 未絕育      2021-04-11 2014-01-15     │
│ 已在所                                │
│ 4561 天（約 12.5 年）                  │
│                                      │
│ 收容所備註                             │
│ (所內絕育)皮病                         │
│                                      │
│ 欄位內容依各收容所登錄實務而異，未填不代表… │
└──────────────────────────────────────┘
```

最大寬 `680px`、最大高 `88vh`，內部可捲。背景遮罩 `rgba(20,16,12,0.55)`，點擊遮罩或按 Esc 關閉，開啟時鎖住背景捲動。

---

## 9. 狀態

每個資料驅動的畫面都有四種狀態，重新設計時都要有對應樣式：

| 狀態 | 呈現 |
|---|---|
| 載入中 | 純文字「載入中…」，弱化色，上下留白 3rem。找動物頁另註明「（全國動物資料約 272 KB）」 |
| 載入失敗 | 「資料載入失敗（錯誤訊息）。」＋重試按鈕 |
| 篩選後為空 | 「沒有符合條件的動物。」 |
| 面板收起（地圖） | 左上角只有一顆「全臺收容所在所數」按鈕，其餘讓給地圖 |
| 收容所位置載入失敗 | 圖釘圖層不出現，choropleth 照常。圖釘是疊層，不該把地圖一起拖下水 |

---

## 10. 響應式

| 斷點 | 變化 |
|---|---|
| `> 900px` | 地圖單欄全寬，左側面板浮在地圖上，開啟時地圖往右讓位 |
| `≤ 900px` | 面板改為一般流排在地圖下方，地圖不再讓位。窄畫面上浮動面板會蓋住它正在描述的那個縣市 |
| `≤ 820px` | 收容所列表列改為兩欄折行，名稱與地址佔滿整行 |
| `≤ 760px` | 地圖頁 KPI 列由六欄降為兩欄，最後一格橫跨整列 |
| 地圖高度 | 上限 `74vh`。全寬時 560×588 的 viewBox 會超過 1,200px 高 |
| 動物卡片網格 | 欄數寫死，只允許 1／2／4／5，跳過 3（見下表） |

動物卡片網格的實際欄數。外框寬 1180px、左右各 1.5rem 內距，所以可用寬度是 `min(100vw, 1180) - 48`，卡片寬度再扣掉每個欄界的 1rem 間距：

| 視窗寬 | 欄數 | 卡片寬 |
|---|---|---|
| `≥ 1180px` | 5 | 214px |
| `1080–1179px` | 5 | 194–213px |
| `740–1079px` | 4 | 161–246px |
| `420–739px` | 2 | 178–338px |
| `< 420px` | 1 | 整列 |

四欄下限訂在 740px，是因為卡片再窄下去 `公 · 中型 · 成體 · 黑色` 那行就會折成第三行。

---

## 11. 畫面四：分析（`/analysis`）

**目的**：回答地圖答不了的那個問題——在所天數的分布長什麼樣。

版面是單欄堆疊的卡片，由上而下：

1. **警語卡**（`--surface-sunk` 底）。**放在圖表上方，不是註腳**——這頁只有在這段先被讀到時才是誠實的
   - 卡片頂端左為「⚠ 方法學指引與資料邊界」膠囊、右為 `DATA BOUNDARY & BIAS`
   - 三項警語排成**三欄**，各有編號 01／02／03、中英並列的術語（Right-Censoring／Length-Biased Sampling／Survival Analysis），以及底部一行狀態標籤。底部用 `margin-top: auto` 對齊，三段長度不同時才不會參差
   - 排成直式編號清單時第三點會被讀成補充；三欄之後三點份量相等
2. **控制列**：對象（全部／狗／貓）、橫軸（對數／線性）、平滑程度（較細／標準／較平滑），右端是**已套用**的摘要，含當前頻寬數值。三組 chip 有四種讀法，這行把答案講一次；頻寬更是唯一「不講出來就看不出影響」的設定
3. **長條圖＋核密度曲線**：長條與曲線共用同一條 y 軸（兩者都是密度），中位數以虛線標示，滑過長條讀出該區間隻數
4. **累積分布（ECDF）**：狗與貓兩條，用 `--series-a/b`。畫成**階梯**不是折線——ECDF 在每個觀測點跳躍，兩隻動物之間畫斜線等於宣稱沒人有過的數值
5. **分位數表**：讓每個數字可以被複製查證，也是圖表之外的替代讀法
6. **各縣市排行**：從地圖頁搬過來的。每列有 01–22 序號，右側**同時顯示兩個指標**（`504 隻 / 1,713 天`），中位數達全國兩倍者用強調色

每張圖下方的說明都有**自己的小標**與凹陷底色（數據判讀與尺度邊界／階梯曲線判讀指引／偏態分布判讀重點／縣市排行資料背景與口徑差異）。無標題的灰字段落在圖下方會被當成圖說跳過，而這頁的推論就寫在那裡面。

### 這頁的三條規矩

- **頻寬要公開**。KDE 的形狀有一半是頻寬的主張，只給一條曲線等於把選擇藏起來。三段頻寬是 Silverman 值的 0.6／1.0／1.7 倍
- **可引用的數字讀 ECDF，不讀 KDE**。ECDF 沒有頻寬也沒有平滑假設，「六成的狗已在所超過一年」不該取決於一個讀者可以拖動的控制項
- **兩種軸都給**。線性軸下密度是單調遞減的長尾；對數軸下才看得到雙峰。同一組數字，說的是不同層次的事，只放好看的那個等於替讀者做了他看不見的選擇
- **門檻要寫出來**。排行榜的強調色門檻（中位數達全國兩倍）寫在說明裡：寫出來的門檻讀者可以不同意，沒寫的門檻就變成對該縣市的指控

### 明確不做的圖

**尋獲地熱區圖。** 尋獲地欄位只有 0.1% 有完整地址，區級覆蓋率 36.2% 且各縣市從 1.1% 到 95.1% 不等。做出來的熱區反映的是各收容所的登錄習慣，不是動物實際被撿到的位置。

---

## 12. 設計約束

改動任何畫面時，以下七條先於美觀：

1. **顏色一律引用 token，不要寫死 hex**，否則深色模式會失效
2. **深色模式的資料色階是反向的**，不能把淺色那組直接套用
3. **保留「這張圖不能拿來說什麼」與分析頁的警語卡**，並維持與內容相當的視覺份量。這是這個專案最有說服力的部分，不是可以裁掉的裝飾
4. **中文字串照抄**，包含「已在所」「在所數」「滯留中位數」這些用詞——它們是刻意選的，「在所」不等於「收容」，「已在所」不等於「等待認養」
5. **不要加 emoji、插畫或吉祥物**
6. **不要出現「立即認養」這類 CTA 按鈕**。本站不辦理認養，只呈現公開資料，導向行動會誤導
7. 數字欄位維持等寬數字對齊（`font-variant-numeric: tabular-nums`）
